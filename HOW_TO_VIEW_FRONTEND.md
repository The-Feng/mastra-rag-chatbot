# 🌐 如何查看前端页面

## 🎯 前端页面 URL

部署成功后，你的前端页面地址是：

```
https://mastra-agent.pages.dev
```

## 📋 访问步骤

### 步骤 1: 确认 Pages 已部署

#### 方法 A: 检查 GitHub Actions

1. 进入你的 GitHub 仓库
2. 点击 **Actions** 标签
3. 查找 **Deploy to Cloudflare Pages** 工作流
4. 确认最新运行状态为 ✅ **绿色**（成功）

#### 方法 B: 检查 Cloudflare Dashboard

1. 访问：https://dash.cloudflare.com/
2. 点击左侧 **Pages**
3. 查找项目：`mastra-agent`
4. 查看部署状态

### 步骤 2: 访问页面

#### 直接访问

在浏览器中打开：
```
https://mastra-agent.pages.dev
```

#### 如果页面不存在

如果访问时显示 404 或页面不存在，说明 Pages 还没有部署。继续下面的步骤。

## 🚀 触发 Pages 部署

### 方法 1: 推送到 master 分支（自动触发）

```bash
cd /Users/gjf/Desktop/project/jcyd-34/new-mastra

# 添加所有文件
git add .

# 提交更改
git commit -m "Trigger Pages deployment"

# 推送到 GitHub
git push origin master
```

推送后，GitHub Actions 会自动：
1. 构建 Workers
2. 部署 Workers
3. 部署 Pages

等待 2-3 分钟后，访问 `https://mastra-agent.pages.dev`

### 方法 2: 手动触发（推荐用于测试）

1. **进入 GitHub 仓库**
2. 点击 **Actions** 标签
3. 在左侧选择 **Deploy to Cloudflare Pages**
4. 点击右上角 **Run workflow** 下拉菜单
5. 选择 **master** 分支
6. 点击 **Run workflow** 按钮
7. 等待部署完成（通常 1-2 分钟）

### 方法 3: 检查部署状态

部署过程中：

1. **GitHub Actions 页面**：
   - 点击运行中的工作流
   - 查看实时日志
   - 等待显示 "Deploy to Cloudflare Pages" 步骤完成

2. **Cloudflare Dashboard**：
   - Pages → `mastra-agent`
   - 查看部署历史
   - 确认最新部署状态

## ✅ 部署成功后的验证

### 1. 访问页面

打开浏览器访问：
```
https://mastra-agent.pages.dev
```

### 2. 应该看到

- ✅ 美观的聊天界面
- ✅ 紫色渐变背景
- ✅ 输入框和发送按钮
- ✅ 侧边栏（对话记录）

### 3. 测试功能

#### 测试聊天功能

1. 在输入框输入：`你好，请介绍一下你自己`
2. 点击 **发送** 按钮
3. 应该看到：
   - 你的消息显示在右侧（蓝色）
   - AI 回复显示在左侧（灰色）
   - 流式响应（文字逐渐出现）

#### 测试 API 连接

1. 打开浏览器开发者工具（按 F12）
2. 切换到 **Network**（网络）标签
3. 发送一条消息
4. 查看是否有 `/api/chat` 请求
5. 确认请求状态为 **200 OK**

## 🔍 故障排除

### 问题 1: 页面显示 404 或 "Not Found"

**原因**：Pages 还没有部署

**解决**：
1. 检查 GitHub Actions 是否运行
2. 手动触发 Pages 部署（方法 2）
3. 等待部署完成（1-2 分钟）

### 问题 2: 页面加载但 API 调用失败

**症状**：页面显示正常，但发送消息时出错

**检查**：
1. 打开浏览器开发者工具（F12）
2. 查看 **Console**（控制台）标签
3. 查看错误信息

**可能原因**：
- `CLOUDFLARE_WORKER_URL` 未设置或错误
- Workers 未部署
- CORS 问题

**解决**：
1. 确认 `CLOUDFLARE_WORKER_URL` 在 GitHub Secrets 中正确设置
2. 确认 Workers 已部署并可访问
3. 检查 Workers URL 格式是否正确

### 问题 3: 页面显示但样式异常

**可能原因**：
- CSS 文件未加载
- CDN 资源加载失败

**解决**：
1. 检查网络连接
2. 查看浏览器控制台错误
3. 尝试刷新页面（Ctrl+F5 或 Cmd+Shift+R）

### 问题 4: 部署一直失败

**检查**：
1. GitHub Actions 日志中的错误信息
2. 确认 Secrets 已正确设置：
   - `CLOUDFLARE_API_TOKEN`
   - `CLOUDFLARE_ACCOUNT_ID`
   - `CLOUDFLARE_WORKER_URL`
3. 确认 `public` 目录存在且包含 `index.html`

## 📱 移动端访问

前端页面支持移动端，可以在手机上访问：

```
https://mastra-agent.pages.dev
```

## 🔗 相关链接

- **Workers URL**: `https://mastra-agent.your-subdomain.workers.dev`
- **Pages URL**: `https://mastra-agent.pages.dev`
- **GitHub Actions**: `https://github.com/your-username/your-repo/actions`
- **Cloudflare Dashboard**: https://dash.cloudflare.com/

## 📋 快速检查清单

- [ ] GitHub Actions 已运行 Pages 部署
- [ ] 部署状态为成功（绿色）
- [ ] 可以访问 `https://mastra-agent.pages.dev`
- [ ] 页面正常显示
- [ ] 可以发送消息
- [ ] API 连接正常（检查 Network 标签）

## 🎯 立即操作

### 如果还没部署：

```bash
# 推送代码触发部署
git add .
git commit -m "Deploy Pages"
git push origin master
```

然后等待 2-3 分钟，访问：
```
https://mastra-agent.pages.dev
```

### 如果已部署：

直接访问：
```
https://mastra-agent.pages.dev
```

---

**现在就去访问你的前端页面吧！** 🎉

