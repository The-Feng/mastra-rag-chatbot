# ✅ 分支配置已更新

## 🔄 已完成的更改

所有 GitHub Actions 工作流已更新为使用 `master` 分支：

- ✅ `.github/workflows/deploy-cloudflare.yml` - Workers 自动部署
- ✅ `.github/workflows/deploy-pages.yml` - Pages 自动部署

## 📋 当前配置

### 自动触发条件

当代码推送到 `master` 分支时，会自动触发：

1. **Workers 部署**：修改 `src/` 目录下的文件时
2. **Pages 部署**：修改 `public/` 目录下的文件时

### 手动触发

你也可以在 GitHub Actions 页面手动触发部署：

1. 进入 GitHub 仓库 → **Actions**
2. 选择对应的工作流
3. 点击 **Run workflow** → **Run workflow**

## 🚀 现在可以部署

```bash
# 推送代码到 master 分支
git add .
git commit -m "Update branch configuration to master"
git push origin master
```

## 📝 注意事项

- ✅ 所有工作流现在监听 `master` 分支
- ✅ 推送到 `master` 会自动触发部署
- ✅ 手动触发功能仍然可用

---

**配置完成！现在推送到 `master` 分支即可自动部署！** 🎉

